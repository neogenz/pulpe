# Tech Debt Audit — Backend Post-Refactor

**Last updated**: 2026-05-09  
**Scope**: Backend post-Tier-3 refactor — actionable findings only.

## Findings

| ID | Module | Category | File:Line | Severity | Effort | Description | Recommendation |
|----|--------|----------|-----------|----------|--------|-------------|----------------|
| **CR-02** | budget-template | Subtle bug | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:380-388` | Critical | S | `countOnboardingTemplatesInWindow` filters on column `is_from_onboarding` which **does not exist** in the schema. PostgREST returns error; code does `count ?? 0 = 0`, so rate limit always passes. Onboarding rate limit silently disabled in prod. | Either add the column (migration + insert path in `create_template_with_lines`) or rewrite as "any template created in last 24h". Add a spec that fails if the filter no longer matches the schema. |
| **HI-03** | encryption | Repo error hygiene | `encryption/infrastructure/persistence/supabase-encryption-key.repository.ts:29,53,69,86,104,126,150,170` | High | S | All 8 methods throw plain `Error` with `userId` interpolated. `GlobalExceptionFilter` returns raw 500 to client with userId. | Wrap with `BusinessException(ERROR_DEFINITIONS.ENCRYPTION_REPOSITORY_FAILURE, …)`. Keep userId out of client-facing message. |
| **HI-05** | encryption | Recovery partial-failure | `encryption/infrastructure/crypto/aes-gcm.crypto-service.ts:444-454` | High | M | `recoverWithKey` re-encrypts data, then final wrap step (lines 453-454) is uncaught. If wrap fails after re-encryption, `wrapped_dek` is left null permanently and recovery key dies. Compare with `changePinRekey` (lines 617-643) which has proper partial-failure handler. Real consequence: recovery key broken, NOT vault locked. | Wrap in try/catch; on failure throw `ENCRYPTION_REKEY_PARTIAL_FAILURE`. Mirror PIN-change pattern. |
| **HI-06** | encryption | Logging info leak | `encryption/infrastructure/crypto/aes-gcm.crypto-service.ts:125-131` | High | S | `tryDecryptAmount` warns with `error.message` on every cross-DEK miss. Floods logs with `Unsupported state or unable to authenticate data` — masks real failures. | Demote to `debug`, drop `error.message`, keep `{ ciphertextLength }`. |
| **HI-07** | budget | Layer violation | `budget/application/find-all-budgets.use-case.ts:35-50` | High | M | Use cases inject `AuthenticatedSupabaseClient` just to call `supabase.auth.getUser()`. Per ADR-0006, application layer must not see raw client. Largest residual Tier-3 leak. | Define `USER_PROFILE_PORT.getPayDayOfMonth(userId)` or expose `payDayOfMonth` on `AuthenticatedUser`. Remove `@SupabaseClient()` from controller read endpoints. |
| **HI-08** | budget | N+1 read | `budget/application/find-all-budgets.use-case.ts:81-103,106-133` | High | L | `enrichBudgetsWithRemaining` per budget calls `calculateEndingBalance` (3 queries) + `getRollover` (1-2 queries) ≈ 5N queries. | Add `fetchBudgetMetricsForUser()` returning all lines+txs grouped by budget_id. Compute rollover once per user. |
| **HI-09** | budget-line | RLS / silent skip | `budget-line/infrastructure/persistence/supabase-budget-line.repository.ts:110-119` | High | S | `fetchBudgetIdForLine` swallows error and returns `null`. `RemoveBudgetLineUseCase` skips `recalculate()` and deletes, leaving `ending_balance` stale and the cache cleared with stale data. Not delayed staleness — the stale balance becomes the new authoritative cached value. Active rug-pull on the cached read path. | Throw `BUDGET_LINE_NOT_FOUND` on error; only return `null` for actual not-found. Use case branches explicitly. |
| **HI-10** | transaction | RLS / silent skip | `transaction/infrastructure/persistence/supabase-transaction.repository.ts:239-248` | High | S | `fetchBudgetIdForTransaction` swallows error and returns `''`. `RemoveTransactionUseCase` skips `recalculate()` on any error. Same shape as HI-09. | Throw `BusinessException` on error; return `null` (not `''`) only for not-found. Use case branches explicitly. |
| **HI-11** | budget-line | Atomicity | `budget-line/application/remove-budget-line.use-case.ts:31-38` | High | S | If `repo.delete(id)` succeeds but `budgetRecalculation.recalculate(...)` throws, line is gone but `ending_balance` is stale. No compensation. | Wrap delete + recalc in one SQL transaction (RPC), or invalidate cache before recalc and log/alert recalc failure. |
| **HI-12** | budget-line | Cross-module discipline | `budget-line/application/create-budget-line.use-case.ts:6,27,41` | High | S | Use cases inject `CurrencyService` directly (concrete class). ADR-0002 mandates ports + tokens. | Define `CURRENCY_PORT` in `currency/domain/ports/`. Bind in `CurrencyModule`. |
| **HI-13** | budget-line | Cross-module reach | `budget-line/infrastructure/persistence/supabase-budget-line.repository.ts:24,213-225` | High | S | Repo imports `TemplateLineRow` from budget-template and selects directly from `template_line` table. | Expose `findById(id)` on `BUDGET_TEMPLATE_REPOSITORY`. Orchestrate two repos in use case. Drop `fetchTemplateLineById` from port. |
| **HI-14** | transaction | Race / atomicity | `transaction/application/toggle-transaction-check.use-case.ts:21-22` | High | M | `toggleCheck` reads then writes in two separate round-trips with no row-lock. Two concurrent toggles can no-op each other. | Push toggle into one SQL `UPDATE … SET checked_at = CASE WHEN checked_at IS NULL THEN NOW() ELSE NULL END WHERE id = $1 RETURNING *`. |
| **HI-15** | transaction | Cache + recalc | `transaction/application/toggle-transaction-check.use-case.ts:21-25` | High | S | Toggle invalidates cache but does NOT call `BudgetRecalculationPort.recalculate`. If `transaction.checked_at` participates in recalc, balance is stale post-toggle. | Verify `transaction.checked_at` semantics. If it participates in recalc, call it. If UI-only, drop cache invalidation too. |
| **HI-16** | user | Layer violation | `user/infrastructure/persistence/supabase-user.repository.ts:9-11,214-227` | High | S | Repo imports Zod schemas and parses them. Settings parsing is a domain invariant per architecture rules. | Move parsing to `UserSettingsParser` in `domain/`. Repo returns raw values. |
| **HI-17** | user | Missing validation | `user/application/update-user-settings.use-case.ts:22-32` | High | S | No `UserInvariants.validateSettingsUpdate(patch)` call. Validation lives only in controller. Any internal caller sends garbage straight to `auth.users.user_metadata`. | Add invariant validation at use case boundary. |
| **HI-18** | user | Idempotent path 401 | `user/application/schedule-account-deletion.use-case.ts:30-42` | High | M | `scheduleAccountDeletion` calls `signOutGlobally` AFTER `scheduleDeletion`. On re-trigger after grace-period start, token is rejected and endpoint throws error even though side effect is moot. | Skip `signOutGlobally` when already scheduled, or catch+warn-log the admin signOut failure. |
| **HI-19** | account-deletion | Cron silent failure | `account-deletion/application/cleanup-expired-deletions.use-case.ts:42-47` | High | S | Catch-all `logger.warn` swallows all errors. A bug silently leaves accounts un-deleted forever. No counter, no alert hook. | Emit `logger.warn({ severity: 'critical', op: '…' }, …)` matchable by Sentry/PostHog. Or expose counter to healthcheck. |
| **HI-20** | demo | Cron unbounded loop | `demo/infrastructure/auth/supabase-demo-credentials.adapter.ts:103` | High | S | Demo cleanup has `while (true)` page loop with no MAX_PAGES guard. Same swallow-and-warn pattern as HI-19. | Add `MAX_PAGES = 100`. Same alerting hook as HI-19. |
| **HI-21** | budget-template | God file | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:1-783` | High | L | 783 LOC, 19 public methods, 5 private encryption helpers, 2 RPC orchestrations, 2 entity mappers. Exceeds 300-LOC rule by 2.6×. | Split into templates + lines repos + RPC encoder. |
| **HI-22** | budget-template | Dead `_supabase` parameter | All 14 use cases in budget-template (also: 4 in budget-line) | High | M | `_supabase: unknown` parameter on every `execute()`. Never read. Controller passes `@SupabaseClient()` into something the use case can't type-check. Leftover from pre-CLS era. | Remove parameter from all signatures. Stop passing `supabase` from controllers. ~18 use cases simplified. |
| **HI-23** | budget-template | RPC payload `as never` | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:479,521,522` | High | S | `p_lines: validated as never` defeats both the Supabase-generated type and the Zod-validated typing. Future schema change would compile but corrupt payloads. | Cast to `Database['public']['Functions']['create_template_with_lines']['Args']` (the generated type). |
| **HI-24** | budget-template | Raw error leak from RPCs | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:482,526` | High | S | Both RPC orchestrations do `if (error) throw error;` — leaks raw Postgrest error. Every other DB call wraps in `BusinessException`. | Wrap in `BusinessException(ERROR_DEFINITIONS.TEMPLATE_CREATE_FAILED, …)`. |
| **HI-25** | budget-template | Silent ignore of update error | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:131-142` | High | S | `resetDefaultTemplates` issues UPDATE and **never destructures `error`**. If update fails, next step proceeds and user can have two default templates. | Capture `{ error }` and throw `TEMPLATE_UPDATE_FAILED` w/ cause. |
| **HI-26** | budget-template | Wide / dead port | `budget-template/domain/ports/budget-template-repository.port.ts:30,63` | High | S | `findById(id, userId)` accepts `userId` but never filters on it. `fetchAllBudgetsForTemplate` unreferenced. Future caller assumes it's access-controlled and won't validate. | Delete both. Use `validateAccess` everywhere. |
| **HI-27** | budget-line | Dead use case | `budget-line/application/find-all-budget-lines.use-case.ts:1-37` | High | S | Use case wired in module + controller but no `@Get()` route invokes it. Full chain shipping unused: use case, port method, repo SQL. | Delete. Drop `findAll()` from port + repo. |
| **HI-28** | transaction | Dead use case | `transaction/application/find-all-transactions.use-case.ts` | High | S | Use case wired in module but no controller route binds to it. | Either add the route or delete + drop module providers. |
| **HI-29** | transaction | Search pattern injection-adjacent | `transaction/application/search-transactions.use-case.ts:79-82` | High | S | `buildSearchPattern` escapes `*.()[\\\]` but not `,`. Pattern interpolated into `.or('name.ilike.${searchPattern},…')`. Query with `,` breaks the `.or()` parser. | Escape `,`, `:`, `(`. Or two `.ilike()` queries unioned. |
| **HI-30** | budget-template | Validate-then-write race | `budget-template/application/create-template.use-case.ts:39-58` | High | M | `countForUser` then `createTemplateWithLines` not atomic. Five parallel requests blow past `MAX_TEMPLATES_PER_USER=5`. | Push limit into SQL function or partial unique constraint/trigger. |
| **HI-31** | account-deletion | Performance / scaling | `account-deletion/infrastructure/persistence/supabase-account-deletion.repository.ts:8-46` | High | M | `listExpiredScheduledUsers` paginates entire `auth.users` table (100k users) and filters in-memory each cron run. Doesn't scale. | Build a SQL view or maintain `scheduled_deletions` sidecar table with expression index. |
| **HI-33** | budget-line | Wrong error mapping | `budget-line/infrastructure/persistence/supabase-budget-line.repository.ts:174-186` | High | S | `update` throws `BUDGET_LINE_NOT_FOUND` for ANY supabase error (constraint violation, RLS denial, network). A 23505 returns 404. | Mirror `insert`: handle `error?.code === '23505'` → `BUDGET_LINE_ALREADY_EXISTS`; default `BUDGET_LINE_UPDATE_FAILED`. |
| **HI-34** | budget-template | Atomicity test missing for SQL | `bulk-template-line-operations.use-case.spec.ts:151-187` | High | M | Spec asserts caller doesn't fall back on RPC reject — good — but never proves the SQL itself rolls back. Migration claims atomicity; pgTAP test fails before INSERT loop runs. | See SQL-01. Add test failing late (after INSERT) and asserts no template_line was mutated. |
| **HI-35** | budget-template | Unscoped refetch in bulk RPC | `budget-template/infrastructure/persistence/supabase-budget-template.repository.ts:541-544` | High | S | After atomic write, refetch is `.from('template_line').select('*').in('id', writtenIds)` — no `template_id` filter. Defends only by RLS. | Add `.eq('template_id', input.templateId)` defense-in-depth. |
| **SQL-01** | sql-tests | Atomicity test gap | `backend-nest/supabase/tests/apply_template_line_operations_failure_rollback.sql:73-79` | High | S | Test failure happens BEFORE INSERT loop. "INSERT did not leak" assertion is vacuous — INSERT was never attempted. | Add scenario where failure occurs AFTER INSERT completes (invalid enum, invalid cast, or budget propagation failure). |
| **SQL-02** | sql-tests | Owner-only test irrelevant | `backend-nest/supabase/tests/create_budget_from_template_owner_only.sql:5-7,33-42` | High | S | Schema enforces `template.user_id NOT NULL`, so test cannot exercise diff (rejecting `user_id IS NULL`). Cross-user rejection was already enforced. Test proves nothing new. | Drop `NOT NULL` inside test, insert `user_id IS NULL` row, assert RPC rejects. Or document as defense-in-depth. |
| **SQL-03** | sql-tests | jsonb_to_recordset trap | `backend-nest/supabase/migrations/20260508120100_apply_template_line_operations_atomic.sql:62-67,128-145,180-181,246-262` | High | S | RPC parses JSONB by hand with `line->>'key'`. Misspelled keys silently return NULL. Server-side has no validation — relies entirely on Zod `.strict()` in repo. | Server-side defense: enumerate known keys and `RAISE EXCEPTION` on unknowns. Or use `jsonb_to_recordset` with explicit columns. |
| **SQL-04** | sql-tests | RPC contract drift | `backend-nest/supabase/migrations/20260508120100_apply_template_line_operations_atomic.sql:91-95,116-120` | High | S | UPDATE LOOP writes columns only when JSON key is present. Zod marks all `.optional().nullable()`. Caller passing `{ id, original_amount: null }` clears; `{ id }` (key absent) preserves. Dual semantics not tested. | Add integration test for both shapes. Document the dual semantics. |
| **SQL-05** | sql-tests | No CI hook | `backend-nest/supabase/tests/README.md` | High | M | SQL tests cannot be run automatically. No `package.json` script or CI step. Combined with SQL-02 regression-protection value is near zero. | Add `"test:db"` script. Wire into CI after `supabase db reset`. |
| **CC-01** | cross-cutting | Stale doc | `backend-nest/CLAUDE.md` (Module Structure section) | High | S | Backend CLAUDE.md describes pre-Tier-3 flat layout, contradicting actual `domain/application/infrastructure` structure. New contributors follow wrong template. | Replace with layered layout from `docs/ARCHITECTURE.md`. |
| **CC-02** | cross-cutting | Doc drift | `backend-nest/src/common/utils/error-handler.ts:13-16` | Medium | S | JSDoc lists call sites from deleted files. Stale. | Update list to current callers or delete list. |
| **CC-04** | cross-cutting | Duplicated `getDek()` | `budget`, `budget-line`, `budget-template`, `transaction` repos | Medium | S | Each repo has byte-identical 3-line `private async getDek()`. ADR-0004 promised centralization. | Either base class `EncryptedSupabaseRepository` or shared helper. |
| **BL-14** | budget-line | Missing test | `budget-line/application/update-budget-line.use-case.ts` (5 of 6 write paths without spec) | High | M | Only `create-budget-line.spec.ts` exists. All other write paths = zero coverage. | Add specs covering invariants, port calls, cache invalidation, recalc invocation. |
