{
  "name": "pulpe-workspace",
  "version": "0.45.1",
  "private": true,
  "description": "Pulpe monorepo workspace",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "postinstall": "lefthook install",
    "dev": "turbo dev",
    "dev:shared": "turbo dev --filter=pulpe-shared",
    "dev:frontend": "turbo start --filter=pulpe-frontend",
    "dev:backend": "turbo build --filter=pulpe-shared && cd backend-nest && bun run dev",
    "dev:android": "turbo dev --filter=pulpe-android",
    "build:android": "turbo build --filter=pulpe-android",
    "build": "turbo build",
    "build:all": "turbo build",
    "build:shared": "turbo build --filter=pulpe-shared",
    "build:frontend": "turbo build --filter=pulpe-frontend",
    "build:backend": "cd backend-nest && bun run build",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "test:unit": "turbo test",
    "test:e2e": "turbo test:e2e",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "format": "turbo format && pnpm format:automation",
    "format:check": "turbo format:check && pnpm format:check:automation",
    "format:automation": "prettier --write \".github/**/*.{yml,yaml,mjs}\"",
    "format:check:automation": "prettier --check \".github/**/*.{yml,yaml,mjs}\"",
    "type-check": "turbo type-check",
    "quality": "turbo quality && pnpm format:check:automation && pnpm test:migration-contract && pnpm test:app-store-build-status && pnpm test:ci-security && pnpm test:public-surface && pnpm test:lexicon",
    "quality:fix": "turbo quality:fix && pnpm format:automation",
    "audit:critical": "pnpm audit --audit-level critical",
    "test:ci-security": "node --test .github/scripts/ci-security.test.mjs",
    "test:app-store-build-status": "ruby .github/scripts/app-store-build-status.test.rb",
    "test:migration-contract": "node --test .github/scripts/migration-contract.test.cjs",
    "test:public-surface": "node --test .github/scripts/public-surface.test.mjs",
    "test:lexicon": "node --test .github/scripts/lexicon.test.mjs",
    "checks": "turbo checks",
    "deps:check": "pnpm --filter pulpe-frontend run deps:circular",
    "shared:watch": "turbo watch --filter=pulpe-shared",
    "shared:build": "turbo build --filter=pulpe-shared",
    "dev:frontend-only": "turbo dev --filter=pulpe-frontend",
    "dev:backend-only": "turbo build --filter=pulpe-shared && cd backend-nest && bun run dev",
    "changeset": "changeset",
    "changeset:version": "changeset version && pnpm install",
    "build:landing": "cd landing && pnpm build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@types/node": "^24.0.4",
    "lefthook": "^1.9.2",
    "prettier": "^3.5.3",
    "turbo": "^2.9.14",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "driver.js": "^1.8.0"
  }
}
