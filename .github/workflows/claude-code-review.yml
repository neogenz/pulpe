name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize]
    paths:
      - 'frontend/**'
      - 'backend-nest/**'
      - 'shared/**'
      - 'supabase/**'
      - 'landing/**'
      - 'ios/**'

jobs:
  claude-review:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build dynamic review prompt
        id: prompt
        run: |
          set -e

          CHANGED_FILES=$(gh pr diff ${{ github.event.pull_request.number }} --name-only) || {
            echo "ERROR: Failed to get PR diff"
            exit 1
          }

          if [[ -z "$CHANGED_FILES" ]]; then
            echo "WARNING: No files changed in PR"
          fi

          echo "Changed files:"
          echo "$CHANGED_FILES"

          # Build dynamic prompt with only relevant stack checklists
          PROMPT="# Code Review - PR #${{ github.event.pull_request.number }}

          Tu es un expert code reviewer. Analyse UNIQUEMENT les fichiers modifiés.

          ## Checklist"

          # Angular 21 (frontend/)
          if echo "$CHANGED_FILES" | grep -q "^frontend/"; then
            PROMPT+="

          ### Angular 21 / Material 21 / Tailwind v4

          **Architecture (CRITIQUE):**
          - \`core/\`: services, guards, interceptors - JAMAIS de composants
          - \`ui/\`: composants stateless réutilisables - JAMAIS inject de services
          - \`feature/\`: domaines métier lazy-loaded - isolation totale entre siblings
          - \`pattern/\`: composants stateful réutilisables cross-feature
          - JAMAIS d'import entre features siblings (extraire vers core/ui/pattern)
          - Components = délégation aux services (logic-free)
          - \`loadChildren\` vers \`.routes.ts\` (pas loadComponent)

          **API Angular:**
          - \`changeDetection: OnPush\` obligatoire
          - Signals: \`signal()\`, \`computed()\`, \`linkedSignal()\`
          - \`input()\`/\`output()\` (pas @Input/@Output)
          - \`httpResource()\` pour GET, \`inject()\` pour DI
          - \`@if\`/\`@for\`/\`@switch\` (pas *ngIf/*ngFor)
          - \`host: {}\` (pas @HostBinding/@HostListener)
          - JAMAIS: \`::ng-deep\`, \`ngClass\`, \`ngStyle\`, \`standalone: true\`
          - Accessibilité: WCAG AA, focus, ARIA"
          fi

          # NestJS 11 (backend-nest/)
          if echo "$CHANGED_FILES" | grep -q "^backend-nest/"; then
            PROMPT+="

          ### NestJS 11 / Bun / Zod 4

          **Architecture Monolithe Modulaire (CRITIQUE):**
          - \`common/\`: cross-cutting (guards, filters, interceptors, decorators, exceptions)
          - \`modules/[domain]/\`: un module par domaine métier isolé
          - Structure module: \`controller.ts\` → \`service.ts\` → \`mapper.ts\` → DB
          - Controllers = routing + validation Swagger (JAMAIS de logique métier)
          - Services = orchestration et logique métier
          - Mappers obligatoires: DTO ↔ Entity (camelCase ↔ snake_case)
          - JAMAIS exposer types \`Database['...']['Row']\` dans réponses API

          **Patterns:**
          - DTOs: \`createZodDto()\` + schemas de \`pulpe-shared\`
          - Auth: \`@AuthGuard()\` + \`@User()\` sur routes protégées
          - Errors: BusinessException, filtres globaux
          - Logging Pino: error(5xx), warn(4xx), info(business)
          - JAMAIS de \`any\` - \`unknown\` si incertain"
          fi

          # Next.js 15 / React 19 (landing/)
          if echo "$CHANGED_FILES" | grep -q "^landing/"; then
            PROMPT+="

          ### Next.js 15 / React 19 / Tailwind v4
          - App Router obligatoire (pas Pages Router)
          - Server Components par défaut, \`'use client'\` uniquement si interactivité
          - \`next/image\` avec dimensions explicites
          - Metadata API pour SEO (\`generateMetadata\`)
          - Data fetching côté serveur (pas useEffect pour fetch)
          - \`loading.tsx\` et \`error.tsx\` pour UX
          - Pas de \`useEffect\` pour data fetching initial"
          fi

          # iOS 17+ / Swift 6 (ios/)
          if echo "$CHANGED_FILES" | grep -q "^ios/"; then
            PROMPT+="

          ### iOS 17+ / Swift 6 / SwiftUI

          **Architecture (CRITIQUE):**
          - \`App/\`: entry point + AppState - JAMAIS de logique métier
          - \`Core/\`: services = \`actor\` (thread-safe), infrastructure
          - \`Domain/\`: models \`Sendable\`, services wrappent APIClient
          - \`Features/\`: Views + ViewModels, sheets pour forms
          - \`Shared/\`: composants réutilisables, extensions, formatters
          - JAMAIS mélanger UI dans Domain layer

          **Patterns:**
          - \`@Observable\` (JAMAIS ObservableObject/@Published)
          - \`async/await\` (JAMAIS completion handlers)
          - \`NavigationStack(path:)\` avec destinations typées
          - Constructor injection avec \`.shared\` defaults
          - JAMAIS force unwrap \`!\` sans justification"
          fi

          # Shared (shared/)
          if echo "$CHANGED_FILES" | grep -q "^shared/"; then
            PROMPT+="

          ### Shared / Zod 4 - Contrats API

          **Rôle (CRITIQUE):**
          - Contrats API entre backend NestJS et frontend Angular
          - Source unique de vérité pour les types échangés
          - Validation runtime côté backend ET frontend

          **Patterns:**
          - Schema + type inféré: \`export const xSchema = z.object({...}); export type X = z.infer<typeof xSchema>;\`
          - Documenter les règles métier en JSDoc (cf. SPECS.md)
          - Nommage: \`entitySchema\`, \`entityCreateSchema\`, \`entityUpdateSchema\`
          - JAMAIS de dépendances externes (sauf zod)
          - JAMAIS de logique métier (seulement validation)"
          fi

          # Supabase (supabase/)
          if echo "$CHANGED_FILES" | grep -q "^supabase/"; then
            PROMPT+="

          ### Supabase / PostgreSQL
          - Migrations idempotentes (\`IF NOT EXISTS\`, \`OR REPLACE\`)
          - RLS policies sur toutes les tables avec \`user_id\`
          - Index sur colonnes fréquemment filtrées
          - Pas de \`CASCADE\` sans justification"
          fi

          # Tests (*.spec.ts, *.test.ts)
          if echo "$CHANGED_FILES" | grep -qE "\.(spec|test)\.ts$"; then
            PROMPT+="

          ### Tests (Vitest / Bun:test / Playwright)

          **Pattern AAA obligatoire:**
          - Arrange: setup des données et mocks
          - Act: exécution de l'action testée
          - Assert: vérification du résultat

          **Principes (CRITIQUE):**
          - Tester le COMPORTEMENT, pas l'implémentation
          - Tests lisibles = documentation vivante
          - Un test = un cas métier clair
          - Helpers: \`createMock...()\` pour factory de données
          - Nommage: \`should [behavior] when [condition]\`
          - JAMAIS tester les détails d'implémentation (ordre d'appels, etc.)
          - JAMAIS de logique conditionnelle dans les tests"
          fi

          PROMPT+="

          ## Règles transversales

          **Clean Code:**
          - Fonctions ≤30 lignes, fichiers ≤300 lignes
          - Nommage: \`isX\`/\`hasX\` pour booléens, pluriel pour collections
          - Constantes nommées (pas de magic numbers)
          - Code auto-documenté (pas de commentaires explicatifs)

          **TypeScript strict:**
          - JAMAIS \`any\` - utiliser \`unknown\` si incertain
          - JAMAIS \`as\` cast sans justification
          - Type guards préférés aux assertions

          **Sécurité:**
          - Pas de secrets hardcodés
          - JWT requis sur routes protégées
          - Validation de tous les inputs externes

          ## Format de sortie (OBLIGATOIRE)

          Utilise EXACTEMENT ce format markdown:

          \`\`\`
          ## Review PR #${{ github.event.pull_request.number }}

          ### 🚨 Critiques (bloquants)
          - \`fichier.ts:42\` - Description du problème
            > Suggestion de fix

          ### ⚠️ Importants
          - \`fichier.ts:15\` - Description du problème
            > Suggestion de fix

          ### 💡 Suggestions
          - \`fichier.ts:88\` - Amélioration possible
            > Suggestion

          ---
          **Verdict:** ✅ APPROVE | ❌ CHANGES REQUESTED
          **Fichiers reviewés:** X fichiers
          \`\`\`

          Règles:
          - Max 10 issues total
          - En français
          - UN SEUL commentaire général (pas de commentaires inline multiples)
          - Si aucun problème: juste le verdict avec félicitations"

          # Output using heredoc for multiline
          echo "review_prompt<<EOF" >> $GITHUB_OUTPUT
          echo "$PROMPT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Claude Code Review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: ${{ steps.prompt.outputs.review_prompt }}
          claude_args: |
            --max-turns 15
            --model claude-sonnet-4-5-20250929
            --allowedTools "Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr files:*)"
