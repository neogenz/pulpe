name: 🚀 Release (DISABLED)

on:
  workflow_dispatch: # Only manual trigger
  # push:
  #   branches: [main]

permissions:
  contents: write
  pull-requests: write
  actions: write

jobs:
  disabled-notice:
    name: 🚫 Release Workflow Temporarily Disabled
    runs-on: ubuntu-latest
    steps:
      - name: 📝 Notice
        run: |
          echo "Release workflow is temporarily disabled"
          echo "Re-enable by uncommenting the 'on: push' trigger"
