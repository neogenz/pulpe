---
description: 
globs: *.ts
alwaysApply: false
---
Private Field Declaration:
- Use `#fieldName` instead of `private fieldName`

Field Access:
- Reference with `this.#fieldName` syntax
- Never expose private fields publicly

Migration Steps:
- Replace `private` keyword with `#`
- Update all field references
