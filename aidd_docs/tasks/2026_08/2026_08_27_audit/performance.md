# Codebase Audit: android/ (performance)

Static review only. Lists are windowed or virtualized, timeouts abort, the React Compiler is on. The one systemic cost is wholesale query invalidation after every mutation.

- **Date**: 2026-08-27
- **Scope**: `android/src` (queries, mutations, lists, charts, startup path)
- **Health**: good
- **Findings**: 0 critical, 1 warning, 2 minor

## Findings

| Sev | Category    | Location                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Issue                                                                                                                                                                                                                                                                                                           | Suggested fix                                                                                                                                                                                                                     | Effort |
| --- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 🟡  | performance | `android/src/features/budgets/toggle-check-mutation.ts:38-39`, `android/src/features/transactions/transaction-mutations.ts:26`, `android/src/features/budget-details/budget-line-mutations.ts:21`, `android/src/features/tags/tag-queries.ts:51`, `android/src/features/templates/template-queries.ts:85`, `android/src/features/savings-goals/goals-queries.ts:149`, `android/src/features/budget-details/spread/spread-queries.ts:30`, `android/src/features/budget-details/savings-withdrawal/withdrawal-mutations.ts:24` | Every mutation invalidates `budgetKeys.all`: the paginated budget list, the periods index and every cached detail refetch after a single pointing tap on the home. `toggle-check` already applies an optimistic update (`:20-33`), so the wholesale invalidation is pure network and a visible refresh flicker. | Invalidate `budgetKeys.detail(budgetId)` (and `periods` when dates change); keep `budgetKeys.all` for budget create/delete only.                                                                                                  | M      |
| 🟢  | performance | `android/src/features/current-month/current-month-queries.ts:58-76`, `android/src/core/vault/vault-store.ts:260`                                                                                                                                                                                                                                                                                                                                                                                                             | Home cold path is a three-request waterfall (settings → periods(year) → details). `lockVault()` clears the whole cache by design (decrypted data), so after every 5-minute lock the home shows a spinner through all three round trips.                                                                         | Prefetch `periods` and the current `details` right after a successful unlock (`unlockVaultWithPin` / biometrics) so the home paints on arrival; or a skeleton hero instead of a centered spinner. No persisted cache (encrypted). | M      |
| 🟢  | performance | `android/src/features/current-month/components/balance-trajectory-chart.tsx:9`, `android/src/features/savings-goals/components/goal-projection-chart.tsx`                                                                                                                                                                                                                                                                                                                                                                    | Two chart engines linked (victory-native + Skia) for two polylines; the APK/AAB size was not measured in this audit.                                                                                                                                                                                            | Measure the release AAB once; if Skia dominates, an SVG or Reanimated polyline covers both charts.                                                                                                                                | L      |

Verified (no row): `reactCompiler: true` (`app.json`); budgets tab is a virtualized `SectionList` with a measured anchor (`budgets.tsx:192-218`); activity is windowed (`activity-window.ts:43`), drift capped at 3 (`drift-card.tsx:21`), unchecked capped (`current-month-view-model.ts:321`); API requests abort on timeout through `AbortController` (`api-client.ts:209-221`) and GETs only are retried; the 2026-08-21 findings "3 s timeout not aborting" and "language change invalidates all queries" are resolved (no locale-driven invalidation left in `src/`).

## Top actions

1. Narrow the invalidation keys (row 1). Hand off to `refactor` + `test`.
2. Prefetch on unlock (row 2) once the routing fix lands, since it changes what the user sees right after the PIN.

## Coverage

- **Scanned**: performance (query/mutation graph, list virtualization, chart stack, startup waterfall, timeout/retry policy)
- **Skipped**: no profiler, static heuristics only; no APK/AAB size measurement; no device run
