# Codebase Audit: android/ (tests)

740 tests green in 82 s and thresholds met, but the suite is shaped so that the one regression users hit (lock → wrong vault screen) is invisible to it: the router is stubbed, the vault screens are rendered alone, and the home components have no render test.

- **Date**: 2026-08-27
- **Scope**: `android/` (124 Jest suites, `maestro/` flows, `.github/workflows/android-e2e.yml`)
- **Health**: fair
- **Findings**: 1 critical, 2 warning, 1 minor

## Findings

| Sev | Category | Location                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Issue                                                                                                                                                                                                                                                                                                                                                                                   | Suggested fix                                                                                                                                                                                                                                      | Effort |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| 🔴  | tests    | `android/src/core/navigation/app-entry.spec.tsx:33`, `android/src/core/vault/vault-routes.spec.tsx`, `android/src/core/vault/auto-lock.spec.ts`                                                                                                                                                                                                                                                                                                                                                        | `Stack.Protected` is stubbed as `guard ? children : null`, so the only test around the gates cannot see react-navigation's fallback; `vault-routes.spec.tsx` renders the three vault screens in isolation with expo-router mocked; `auto-lock.spec.ts` covers `shouldLockOnResume` only. The lock → route transition has no test at any level, which is why the PIN regression shipped. | One integration spec with `renderRouter` from `expo-router/testing-library` (not used anywhere in the repo yet): unlocked on `/home` → `lockVault()` → expect the unlock screen; key rejection → same; fresh account after sign-in → setup screen. | M      |
| 🟡  | tests    | `android/src/features/current-month/components/*` (0 % stmts), `android/src/app/(main)/goal/[id].tsx` (0 %), `android/src/app/(main)/template/[id].tsx` (0 %), `android/src/features/onboarding/steps/*` (0 %), `android/src/features/budget-details/spread/components/*` (0 %), `android/src/features/budget-details/savings-withdrawal/components/*` (0 %), `android/src/features/savings-goals/components/simulator/*` (0 %), `android/src/app/(main)/settings/*` (17 %), `android/src/ui/*` (37 %) | User-facing surfaces with no render test. `home-screen.spec.tsx:22-50` replaces `react-native-paper` wholesale with bare `Pressable`s, so the home page is tested as orchestration only; combined with 0 % on its components, the homepage has no rendering coverage at all.                                                                                                            | RNTL render tests for `HomeHeroCard`, `UncheckedOperationsCard` and `PinPad` first (the three surfaces touched daily), then the two detail screens.                                                                                                | L      |
| 🟡  | tests    | `android/src/core/ui/{amount,card,eyebrow,filter-chip,notice,pill,ripple,sheet,status-badge,emphasis,fab-clearance,keyboard-inset,haptics,field-error,scheme-colors,amount-format}.spec.ts` (16), `android/src/core/i18n/phase*-i18n.spec.ts` + `settings-*-i18n.spec.ts` + `system-surfaces-i18n.spec.ts` (17), `analytics-properties.spec.ts`, `root-provider-order.spec.ts`, `screen-insets.spec.ts`: 36 of 124 suites                                                                              | Source-text specs (`readFileSync` + regex on the component file). They pin how a file is written, not what it renders: a behaviour-preserving refactor breaks them, a broken render keeps them green.                                                                                                                                                                                   | Keep the i18n ones as lexicon guards; convert the 16 `core/ui` ones to RNTL renders, `amount`, `notice`, `filter-chip` first.                                                                                                                      | M      |
| 🟢  | tests    | `.github/workflows/android-e2e.yml:123` (`maestro test android/maestro/smoke.yaml`), `android/maestro/{login-vault,onboarding,check-operation,i18n}.yaml`                                                                                                                                                                                                                                                                                                                                              | Five Maestro flows exist, CI runs one. The vault flow has a script and no runner.                                                                                                                                                                                                                                                                                                       | Add `login-vault.yaml` to the CI job; expose an `AUTO_LOCK_DELAY_MS` override for test builds so the background-return case can be scripted (Maestro cannot wait 5 min).                                                                           | M      |

Verified (no row): 124 suites / 740 tests pass in 82 s with `--runInBand`; coverage 51.66 / 49.77 / 43.37 / 51.45 against thresholds 49 / 48 / 42 / 49 plus per-file floors on `session-store`, `vault-store`, `api-client` (`jest.config.js`); no `.skip`/`.only`; no flakiness observed in this run. Pyramid: unit-heavy, one e2e flow in CI, zero component-level tests through the real router.

## Top actions

1. Router-level regression test for lock/rejection/setup (row 1). Hand off to `test`.
2. Render tests for the three daily surfaces (row 2), then convert the `core/ui` source-text specs (row 3).
3. Run `login-vault.yaml` in CI (row 4).

## Coverage

- **Scanned**: tests (`pnpm --filter pulpe-android test` with coverage, spec inventory by kind, mocks in the routing and home specs, Maestro flows and the e2e workflow)
- **Skipped**: flakiness across repeated runs (single run only); e2e flows not executed locally (no emulator attached)
