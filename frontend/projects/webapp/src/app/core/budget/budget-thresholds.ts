/**
 * Percentage of budget consumed above which the UI shows a warning state.
 * Used by both the dashboard hero and the budget details financial overview.
 */
export const BUDGET_WARNING_THRESHOLD_PERCENT = 90;
