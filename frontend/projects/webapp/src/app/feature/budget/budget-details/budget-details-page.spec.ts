import { describe, it } from 'vitest';

describe('DetailsPage', () => {
  // Due to Angular 21 input.required() limitations with TestBed,
  // this component is fully tested through E2E tests.
  // See e2e/tests/features/budget-details.spec.ts

  it.todo(
    'should be tested via E2E (see e2e/tests/features/budget-details.spec.ts)',
  );
});
