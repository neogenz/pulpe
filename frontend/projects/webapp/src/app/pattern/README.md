# `pattern/`

Rules for this layer live in `.claude/rules/00-architecture/layer-pattern.md` and load
automatically when editing files here. The dependency graph is enforced by
`eslint-plugin-boundaries` in `frontend/eslint.config.js` — that config is the authority.
