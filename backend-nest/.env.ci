# backend-nest/.env.ci
# Environment for CI/CD with local Supabase
NODE_ENV=test
PORT=3000
FRONTEND_URL=http://localhost:4200

# Supabase configuration (REQUIRED - all keys must be injected from GitHub Secrets)
# SUPABASE_URL - must be injected via workflow
# SUPABASE_ANON_KEY - must be injected via workflow
# SUPABASE_SERVICE_ROLE_KEY - must be injected via workflow

# Rate Limiting
THROTTLE_TTL=1000
THROTTLE_LIMIT=100

# Turnstile - REQUIRED (verification automatically skipped in test environment)
# TURNSTILE_SECRET_KEY - must be injected via workflow
